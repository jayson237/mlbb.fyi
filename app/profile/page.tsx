import getCurrentUser from "@/lib/actions/getCurrentUser";
import { NextResponse } from "next/server";

const Page = async () => {
  return (
    <div>
      <h1>Profile</h1>
    </div>
  );
};

export default Page;
