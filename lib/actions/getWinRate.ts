export default async function getWinRate(accId: string) {
  try {
    // add new logic using data api
    return {
      // dummy data
      totalClassic: 0,
      totalRanked: 0
    }
  } catch (error) {
    return null;
  }
}
